<resources>
	<!-- Minimal platform theme: Compose draws the whole UI and edge-to-edge is
	     enabled in code. We only need a no-action-bar launch window with
	     transparent system bars. -->
	<style name="Theme.Concentric" parent="@android:style/Theme.Material.NoActionBar">
		<item name="android:windowBackground">@color/window_background</item>
		<item name="android:statusBarColor">@android:color/transparent</item>
		<item name="android:navigationBarColor">@android:color/transparent</item>
	</style>
</resources>
